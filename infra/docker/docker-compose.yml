services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: alertify
      POSTGRES_USER: alertify
      POSTGRES_PASSWORD: alertify
    ports:
      - "5432:5432"
    volumes:
      - alertify_pgdata:/var/lib/postgresql/data

volumes:
  alertify_pgdata:
